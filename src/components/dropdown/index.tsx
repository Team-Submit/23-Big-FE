import * as S from "./style";

type DropDownListProps = {
  list?: string[];
  type?: "DEFAULT" | "CATEGORY" | "TRANSACTION";
};

export const DropDown = ({ list = [] }: DropDownListProps) => {
  return <S.Container>
    zxc
  </S.Container>;
};
